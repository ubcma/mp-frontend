import { useQuery } from '@tanstack/react-query';

// drizzle query for ORM forwarded by the api/route to the router in the backend 

// will be imported by the actual page.tsx of whatever purchase page it is in terms of rendering user profile
// admin dashboard 