'use client';
import { useQuery } from '@tanstack/react-query';

export default function Home() {

  return (
    <div>
      Admin Access to Stripe Transactions (connected to Drizzle ORM, connected to db)
    </div>
  );
}